debug

sheet "sprites"
  allow-rotate true
  padding 3
  scale 0.25
  output "out/spright-{0-}.png"

description "out/phaser.json"
  template "phaser.inja"

description "out/phaser2.json"
  template "phaser2.inja"

description "out/pixijs.json"
  template "pixijs.inja"

id "{{ source.filename }}"

tag anim "{{ source.dirname }}"

path "images"
glob "**/*.png"

